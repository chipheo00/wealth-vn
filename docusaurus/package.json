{
  "name": "wealthfolio-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Docusaurus documentation service for WealthVN",
  "scripts": {
    "dev": "docusaurus start --port 3001",
    "start": "docusaurus start --port 3001",
    "build": "node scripts/build.cjs",
    "serve": "docusaurus serve",
    "sync": "node scripts/sync-docs.cjs",
    "sync:wiki": "node scripts/sync-wiki.cjs",
    "generate-api": "node scripts/generate-api-docs.cjs",
    "clean": "rm -rf build .docusaurus"
  },
  "dependencies": {
    "@docusaurus/core": "^3.9.2",
    "@docusaurus/preset-classic": "^3.9.2",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
